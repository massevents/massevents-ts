/// <reference types="node" />
import { Request, Response } from 'express';
import EventEmitter from 'events';
export declare function useEventsAsSSE(emitter: EventEmitter, events: string[]): (req: Request, res: Response) => boolean;
