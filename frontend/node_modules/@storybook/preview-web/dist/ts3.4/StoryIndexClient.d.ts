import { StoryIndex } from '@storybook/store';
export declare class StoryIndexClient {
    source: EventSource;
    constructor();
    addEventListener(event: string, cb: (...args: any[]) => void): void;
    fetch(): Promise<StoryIndex>;
}
